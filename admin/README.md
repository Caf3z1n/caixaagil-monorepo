# Admin Caixa Ágil

Painel administrativo interno do Caixa Ágil.

## Comandos

```powershell
npm --prefix admin run dev
npm --prefix admin run typecheck
npm --prefix admin run build
```

O servidor local roda em `http://localhost:3001`.
